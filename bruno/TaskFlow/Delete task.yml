info:
  name: Delete task
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:3000/tasks/:id
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
